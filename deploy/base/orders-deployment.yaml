apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders
  namespace: retailflow
  labels:
    app.kubernetes.io/name: orders
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/part-of: retailflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: orders
      app.kubernetes.io/part-of: retailflow
  template:
    metadata:
      labels:
        app.kubernetes.io/name: orders
        app.kubernetes.io/version: "1.0.0"
        app.kubernetes.io/part-of: retailflow
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      serviceAccountName: orders
      containers:
        - name: orders
          image: quay.io/rdiazgav/retailflow-orders:1.0.0
          ports:
            - containerPort: 8080
              protocol: TCP
          env:
            - name: DB_URL
              value: "jdbc:postgresql://postgresql-orders:5432/orders"
            - name: DB_USER
              value: "orders"
            - name: DB_PASSWORD
              value: "orders"
            - name: OTEL_ENDPOINT
              value: "http://otel-collector:4317"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /q/health/live
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /q/health/ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3
