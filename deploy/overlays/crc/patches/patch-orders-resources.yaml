apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders
  namespace: retailflow
spec:
  template:
    spec:
      containers:
        - name: orders
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 384Mi
