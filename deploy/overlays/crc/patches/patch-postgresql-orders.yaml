apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgresql-orders
spec:
  template:
    spec:
      volumes:
        - name: data
          emptyDir: null
          persistentVolumeClaim:
            claimName: postgresql-orders-pvc
