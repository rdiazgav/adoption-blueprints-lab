apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: retailflow
spec:
  template:
    spec:
      containers:
        - name: api-gateway
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 384Mi
