apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgresql-catalog
spec:
  template:
    spec:
      volumes:
        - name: data
          emptyDir: null
          persistentVolumeClaim:
            claimName: postgresql-catalog-pvc
