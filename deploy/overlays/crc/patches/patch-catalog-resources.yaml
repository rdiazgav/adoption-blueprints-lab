apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog
  namespace: retailflow
spec:
  template:
    spec:
      containers:
        - name: catalog
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 384Mi
