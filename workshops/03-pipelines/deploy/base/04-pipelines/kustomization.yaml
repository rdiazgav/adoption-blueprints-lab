apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: retailflow
resources:
  - quarkus-service-pipeline.yaml
  - generic-service-pipeline.yaml
# PipelineRun examples are applied manually, not managed by Kustomize:
#   oc create -f catalog-pipeline-run.yaml
#   oc create -f recommendations-pipeline-run.yaml
