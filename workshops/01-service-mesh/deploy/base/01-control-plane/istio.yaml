apiVersion: sailoperator.io/v1
kind: Istio
metadata:
  name: default
spec:
  namespace: istio-system
  updateStrategy:
    type: InPlace
  version: v1.28.4
  values:
    meshConfig:
      discoverySelectors:
        - matchLabels:
            istio-discovery: enabled
